# Pinned to the interpreter + package set that produced the published results (Python 3.12.0).
# CI installs this file directly (see .github/workflows/ci.yml, job "test (pinned)") so the
# reproducibility claim is actually gated rather than assumed.
#
# Security posture for these pins is documented in SECURITY.md. Summary: `requests` is kept
# current with all known advisories fixed; `torch` is held at the version that produced the
# results because its outstanding advisories are unreachable from this codebase (no torch.load,
# no load_state_dict, no distributed/RPC) and one of them has no fixed version at all.
numpy==1.26.4
pandas==2.2.3
scipy==1.15.2
scikit-learn==1.6.1
statsmodels==0.14.4
torch==2.13.0      # clears 13 of the 14 advisories that affected 2.7.0; see SECURITY.md
matplotlib==3.10.1
seaborn==0.13.2
tqdm==4.67.1
yfinance==0.2.57
requests==2.34.2   # >=2.33.0 clears PYSEC-2026-1872 (.netrc leak) and PYSEC-2026-2275
